# Uncomment the next line to define a global platform for your project
#platform :ios, '9.0'

target 'node' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'BitcoinCashKit'
  pod 'web3swift', '~> 0.8.0'
  pod 'Firebase/Core'
  pod 'Firebase/Auth'

  # Pods for node

  target 'nodeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'nodeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
